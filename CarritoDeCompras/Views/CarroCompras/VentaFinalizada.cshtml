<!DOCTYPE html>
<html>
<head>
    <title>Boleta</title>
    <style>
        .boleta {
            width: 300px;
            padding: 20px;
            border: 1px solid #ccc;
            font-family: Arial, sans-serif;
        }

        .boleta-header {
            text-align: center;
            margin-bottom: 20px;
        }

        .boleta-title {
            font-size: 20px;
            font-weight: bold;
        }

        .boleta-info {
            margin-bottom: 10px;
        }

            .boleta-info span {
                font-weight: bold;
            }

        .boleta-item {
            margin-bottom: 5px;
        }

            .boleta-item span {
                display: inline-block;
                width: 100px;
            }

        .boleta-total {
            margin-top: 20px;
            text-align: right;
        }
    </style>
</head>
<body>
    <div class="boleta">
        <div class="boleta-header">
            <div class="boleta-title">Boleta de Compra</div>
            <div class="boleta-info">
                <span>Fecha:</span> 01/07/2023
            </div>
            <div class="boleta-info">
                <span>Cliente:</span> John Doe
            </div>
        </div>

        <div class="boleta-item">
            <span>Producto:</span> Zapatos
        </div>
        <div class="boleta-item">
            <span>Precio:</span> $50.00
        </div>
        <div class="boleta-item">
            <span>Cantidad:</span> 2
        </div>

        <div class="boleta-total">
            <span>Total:</span> $100.00
        </div>
    </div>
</body>
</html>